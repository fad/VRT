using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReplayNetworkSetup : MonoBehaviour
{ 
    void Start()
    {
        //if (GameObject.FindGameObjectWithTag("ReplayPlayerClone") != null)
        //    Destroy(GameObject.FindGameObjectWithTag("ReplayPlayerClone"));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
